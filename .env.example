# Local dev only: copy to .env.local and fill in from your Slack Incoming Webhook.
# Use only the path part of the URL (after https://hooks.slack.com), e.g. /services/T000/B000/secret
SLACK_WEBHOOK_PATH=
